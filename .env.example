# Copy this file to `.env` and fill in the values for your environment.

# Backend configuration
BACKEND_ENVIRONMENT=development
BACKEND_LOG_LEVEL=INFO
BACKEND_SENTRY_DSN=
BACKEND_SENTRY_TRACES_SAMPLE_RATE=0.1
BACKEND_METRICS_NAMESPACE=myclosetb_backend

# Frontend configuration
# Supabase project configuration (Project Settings → API).
VITE_SUPABASE_URL="https://your-project-ref.supabase.co"
VITE_SUPABASE_ANON_KEY="your-anon-public-anon-key"

# Optional monitoring hooks used by the React application.
VITE_SENTRY_DSN=""
VITE_METRICS_ENDPOINT=""

# API surface exposed by the backend (defaults to the local reverse proxy).
VITE_API_URL="http://localhost:8000"
